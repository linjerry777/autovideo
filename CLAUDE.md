# CLAUDE.md

This project uses the same working instructions as `AGENTS.md`.

Read `AGENTS.md` first. The key point: the active UI is the FastAPI static UI
served from `web/static` at `http://localhost:9000/ui`. The old Next.js
`frontend/` dashboard has been removed from this repository and should not be
used for current work.
