# ── 核心 ──────────────────────────────────────────────────────────────────────
openai>=1.30.0
httpx>=0.27.0
requests>=2.31.0
ffmpeg-python>=0.2.0
apscheduler>=3.10.0
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
tenacity>=8.2.0
typer>=0.12.0

# ── AI 新聞 pipeline ──────────────────────────────────────────────────────────
anthropic>=0.30.0          # Claude API（整理新聞）
playwright>=1.44.0         # Chromium headless（get_x_cookies 選用）
# tweepy 已移除 — 改用免費 RSS，不需要 X API Token
